# .gitattributes based on GitHub line endings example at
# https://tinyurl.com/3jtkv9fd

# Set the default behavior, in case people don't have core.autocrlf set.

* text=auto

# Explicitly declare text that should normalized and converted to native line
# endings on checkout.
# E.g: *.c text

# Files that will always have CRLF line endings on checkout.

*.dpr text eol=crlf
*.dpk text eol=crlf
*.pas text eol=crlf
*.inc text eol=crlf

# Files that are truly binary and should not be modified.

*.png binary
*.jpg binary
*.gif binary
*.bmp binary

*.res binary
*.rtf binary
